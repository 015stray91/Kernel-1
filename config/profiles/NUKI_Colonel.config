# KernelX Profile - NUKI Colonel
# AnyRealm Unified Kernel Image, Colonel variant
# Target: Motorola Moto G Stylus 5G 2023 (SM6450 parrot)
# Save as: NUKI_Colonel

# Kernel Identity
KERNEL_NAME=NUKI Colonel
KERNEL_VERSION=2.0.0
KERNEL_SOURCE_URL=https://github.com/015stray91/kernel-msm-1
KERNEL_BRANCH=android-14-release-u1tgns34.42-86-2-32

# Build Configuration
BUILD_ARCH=arm64
BUILD_CROSS_COMPILE=aarch64-linux-gnu-
BUILD_OUTPUT_DIR=output
BUILD_TOOLCHAIN_PATH=/usr/bin
ADVANCED_CUSTOM_DEFCONFIG=ksu-nh.defconfig
ADVANCED_EXTRA_CFLAGS=-DCONFIG_KERNEL_LZ4=y

# Device
DEVICE_NAME=parrot
DEVICE_CODE=parrot
DEVICE_PLATFORM=qcom
DEVICE_FAMILY=motorola

# KernelSU Colonel pillars
# - KernelSU-Next + SUSFS + ZeroMount + KPM EUD + ANBU
# - KALLSYMS + FTRACE + FUNCTION_TRACER + DYNAMIC_FTRACE
# - LZ4 kernel compression, ZRAM w/ LZ4KD
# - OverlayFS + FUSE_DAX + TMPFS_XATTR
# - HIDELKM + KPM + MODVERSIONS + MODULE_UNLOAD

# Ramdisk
RAMDISK_COMPRESSION=lz4
RAMDISK_FORMAT=cpio

# Safety
SAFETY_BACKUP_BEFORE_PATCH=true
SAFETY_VERIFY_BUILD=true

# Logging
LOG_LEVEL=INFO
LOG_FILE=kernelx.log

# Toolchain
# Ubuntu 22.04: clang-12 / lld-12 / llvm-12 + gcc-aarch64-linux-gnu
# KSU_VARIANT=KernelSU-Next (set via env or as part of build step)
